import { Cliente } from './../cliente/cliente';
import { Evento } from './../evento/evento';
export class Venda {
   
	cliente: Cliente;
	evento: Evento;	
	quantidade: number;
    valor: number;
    
    
}
